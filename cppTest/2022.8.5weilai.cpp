#include <iostream>
#include <vector>

using namespace std;

int main() {
    /*
    第二题
    */
    int t;
    cin >> t;
    vector<int> nums;
    for (int i = 0; i < t; ++i) {
        int te;
        cin >> te;
        nums.push_back(te);
    }
    cout << 1 << endl;
    return 0;
}